import { Box, styled } from '@mui/material';

export const BoxContainer = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  backgroundColor: '#fff',
  gap: 16,
  padding: 16,
  borderRadius: 16,
}));
