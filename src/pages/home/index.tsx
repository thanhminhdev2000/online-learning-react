import Navbar from '@components/navbar';
import { Background } from '@pages/home/styled';

const HomePage = () => {
  return (
    <Background>
      <Navbar />
    </Background>
  );
};

export default HomePage;
